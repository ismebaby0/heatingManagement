<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.neusoft.ht.SystemSafety.mapper.IFunctionMapper">

 <!-- 没有关联属性的结果映射 -->
  <resultMap id="UserResultMap" type="Function" >
  	<id property="funNo" column="UUSERID" />
  	<result property="funName" column="UPASSWORD" />
   <result property="funUrl" column="UNAME" />
    <result property="levelNo" column="UNAME" />
  </resultMap>
  
 <insert id="create">
   insert into HT_Function values (#{FUNNO},#{FUNNAME},#{FUNURL},#{LevelNo})
 </insert>
 
  <update id="update">

  </update>
  
  <delete id="delete">
 
  </delete>
   
 <select id="selectWithAll" resultMap="UserResultMap">

 </select>
 
  <select id="selectById" resultMap="UserResultMap">

 </select>
 

</mapper>

